.title-bar
  = image_tag "castle.jpg"
  .centered-title
    %h1 Kingdom of Betrayal Sheets
    %h2 A TTRPG Character Storage Tool
    - if current_user
      %h3
        Signed in as
        = current_user.username
      = link_to "View Characters", characters_path, class: "action-button"
      = link_to "Sign Out", session_path, method: :delete, class: "action-button"
    - else
      = link_to "Sign In", signin_path, class: "action-button"
      = link_to "Sign Up", signup_path, class: "action-button"

%center
  .body-content
    %h3 All Your KoB Characters in One Place
    %p Never lose track of your Kingdom of Betrayal characters' information again.

    %h4 Features
    %ul
      %li Store unlimited KoB character sheets
      %li Customize stored characters at any time
      %li Attach character images
      %li 100% free
